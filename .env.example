# === Required ===

# Your Matrix homeserver URL
HOMESERVER_URL=https://matrix.example.com

# Bot account access token (see README for how to get this)
BOT_ACCESS_TOKEN=syt_...

# The rules room where the bot watches for reactions
TARGET_ROOM_ID=!abc123:example.com

# The event ID of the rules message users must react to
TARGET_EVENT_ID=$eventid123

# The invite-only space users get invited to after accepting rules
INVITE_SPACE_ID=!xyz789:example.com

# === Optional ===

# Repost the rules message every N joins (default: 10)
# REPOST_EVERY_N_JOINS=10

# Device ID for the bot's Matrix session (default: GATEKEEPER)
# BOT_DEVICE_ID=GATEKEEPER
